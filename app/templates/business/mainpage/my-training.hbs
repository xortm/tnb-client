<div id="MyTrainingContainer" class="mainContent {{if global_pageConstructure.showLoader 'hide' ''}}">
  <div class="wrapperInner">
    {{#ui/mobile/gestures-container}}
    <div class="width93B margin0auto remarkContentClass">
      <div class="title-box top10 bottom15">
        <h1 class="font-black font24 line-height-30">{{shareDocument.name}}</h1>
        <div class="clearfix color666666 font14">
          <div class="pull_left">分类:{{shareDocument.type.name}}</div>
        </div>
      </div>

      <!-- <div class="width100B">
        {{#each shareDocument.file as |file|}}
          {{#if (eq file.delStatus 0)}}
          <div>
            <span class="line-height-30 font14 color666666">{{file.name}}</span>&nbsp;&nbsp;<a class="pointer" href={{file.path}}>下载</a>
          </div>
          {{/if}}
        {{/each}}
      </div> -->

    </div>
    {{/ui/mobile/gestures-container}}
  </div>
</div>
{{ui/container/infinite-scroll moniteData="" didRenderAction="didRender" didInsertElementAction="didInsertElement"}}
